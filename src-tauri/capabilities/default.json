{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for UniERP Desktop's single window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "deep-link:default",
    "opener:default"
  ]
}
